# price = 25
# print(price > 10 and price < 30)  # when putting and the entire expression has to make sense for it to be true
# print(price > 10 or price < 30)  # when putting or, only one of them have to be true to answer true
# there is AND, OR, and NOT
# AND (both expressions have to be true)
# OR (at least one of the expressions have to be true)
# NOT (which inverses any value given)
